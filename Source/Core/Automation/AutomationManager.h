#pragma once

#include <JuceHeader.h>

/// Manage automation collections for an edit or track.
class AutomationManager {
public:
    /// Create an empty automation manager.
    AutomationManager();
    ~AutomationManager() = default;

private:
};
